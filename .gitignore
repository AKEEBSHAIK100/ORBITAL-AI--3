node_modules/
dist/
build/
.cache/
.env*
!.env.example
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Python
__pycache__/
*.py[cod]
*$py.class
*.pt
!backend/models/building_model.pt

# Large model weight files — never commit to Git
*.safetensors
*.bin
*.gguf
*.ggml
backend/models/generalist/

# Large dataset directories
backend/data/levir_cd/
backend/data/optical_sar/
backend/data/output_bitemporal.json

# Scratch / one-off scripts
dl_optical_sar.py
dl_sar_v2.py
e2e_real_sar.py
sar_test.py
sar_test_bc.py
scratch_step14.py
scratch_step14_res.json
scratch/
.pytest_cache/

# Test & scratch images (keep public assets; exclude raw satellite data)
*.jpg
*.jpeg
*.png
!public/**

