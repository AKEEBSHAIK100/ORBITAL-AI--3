# Core web framework
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Computer vision & numerics
numpy>=1.24.0
opencv-python-headless>=4.8.0
rasterio>=1.3.0
pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# BigEarthNet v2.0 classifier (reBEN — Clasen et al., IGARSS 2025)
# Loads pretrained ResNet-50 from HuggingFace: BIFOLD-BigEarthNetv2-0/resnet50-s2-v0.2.0
# Note: configilm requires pydantic<2.0 which conflicts with FastAPI/pydantic>=2.0.
# ben_classifier.py reports the classifier as unavailable when configilm/model dependencies are missing; no heuristic fallback is used.
huggingface-hub>=0.23.0
timm>=0.9.0
transformers>=4.40.0
peft>=0.10.0

# Phase 4: Optional PostgreSQL connectivity (non-fatal if absent)
# Install manually: pip install asyncpg psycopg2-binary
# asyncpg>=0.29.0
# psycopg2-binary>=2.9.0
